const multiplyByTwo = (number) => {
    return number * 2
}
console.log(multiplyByTwo(4)); // output 8


const multiplicator = function (number, multiply) {
    return number * multiply
}

console.log(multiplicator(8, 2)); // 16