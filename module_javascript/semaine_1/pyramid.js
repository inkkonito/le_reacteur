const lines = 5;
let result = "";
for (let i = 0; i <= lines; i++) {
    result+="#"
    console.log(result)
}

// make a pyramid of # in 5 lines
console.log("\n Second pyramid");

let maxLines = 5
let init = "";

for(let i=0; i<=maxLines; i++) {
    init += "#";
    console.log(init);
}