const arr = [1, 2, 3];

// Début de votre code

const addElement = (arr, elem) => {
    arr.push(elem);
    return arr
}
// Fin de votre code

console.log(addElement(arr, 4)); // Affiche : [1, 2, 3, 4]a